import 'package:flutter/material.dart';

// ── Backgrounds ──────────────────────────────────────────────────────────────
const Color appBg         = Color(0xff0B0D14); // main scaffold background
const Color surfaceBg     = Color(0xff141720); // cards, bottom nav
const Color elevatedBg    = Color(0xff1C1F2E); // elevated cards / calendar bg
const Color navBg         = Color(0xff111318); // bottom navigation bar

// ── Text ─────────────────────────────────────────────────────────────────────
const Color whiteColor    = Color(0xffFFFFFF);
const Color greyColor     = Color(0xff6B7280); // secondary / hint text
const Color subtleGrey    = Color(0xff9CA3AF); // tertiary text

// ── Accent – Green (today ring, dot, weight change, footer border) ────────────
const Color accentGreen   = Color(0xff4ADE80);

// ── Accent – Teal (workout card border, calories bar, arm-workout tag) ────────
const Color accentTeal    = Color(0xff2DD4BF);

// ── Accent – Blue (hydration bar, training-calendar header line) ──────────────
const Color accentBlue    = Color(0xff3B82F6);
const Color lightBlue     = Color(0xff38BDF8); // hydration % text

// ── Accent – Orange (leg-workout border & tag) ────────────────────────────────
const Color accentOrange  = Color(0xffF97316);

// ── Avatars ───────────────────────────────────────────────────────────────────
const Color avatarPurple  = Color(0xff7B5EA7); // R avatar
const Color avatarViolet  = Color(0xff9B59B6); // M avatar

// ── Misc ──────────────────────────────────────────────────────────────────────
const Color toastBg       = Color(0xff0D9488); // water-added toast
const Color dividerColor  = Color(0xff1F2333); // subtle dividers

// ── Mood ring arc colors (left → right around the ring) ──────────────────────
const Color moodRed       = Color(0xffF87171);
const Color moodOrange    = Color(0xffFB923C);
const Color moodYellow    = Color(0xffFBBF24);
const Color moodGreen     = Color(0xff86EFAC);
const Color moodTeal      = Color(0xff5EEAD4);
const Color moodSky       = Color(0xff7DD3FC);
const Color moodLavender  = Color(0xffC4B5FD);
const Color moodPink      = Color(0xffF9A8D4);

// ── Legacy (kept for backward compat) ────────────────────────────────────────
const Color mainColor             = Color(0xff1C373D);
const Color extBg                 = Color(0xff1E2236);
const Color backgroundColor       = Color(0xff070C27);
const Color palmColor             = Color(0xff13111C);
const Color buttonGrey            = Color(0xff515568);
const Color busyColor             = Color(0xff13111C);
const Color orangeColor           = Color(0xffFF9110);
const Color reeColor              = Color(0xffE2384C);
const Color borderColor           = Color(0xff1F4EA4);
const Color buttonColor           = Color(0xff1E2237);
const Color textfieldBackground   = Color(0xff1A1E30);
const Color sosendedBackgroud     = Color(0xff393D52);
const Color gradient1             = Color(0xffE2FC66);
const Color gradient2             = Color(0xffC1F321);
const Color gradient3             = Color(0xffABE824);
const Color greenColor            = Color(0xff8FB172);
const Color darkGreen             = Color(0xff69755B);
const Color navbarColor           = Color(0xff373B4F);
const Color cardColor             = Color(0xff31494E);
const Color fbGoogleBackground    = Color(0xff334C50);
const Color divierColor           = Color(0xff979AAC);
const Color greyColor2            = Color(0xffBEBEBE);
